RULE checkInventory 
WHEN UPDATE OCCURS ON Store AS t
if (t.productCount < 50) {
    var requestCount = 100 - t.productCount
    PUBLISH {"storeId": t.storeId, "productCount":requestCount} TO TOPIC "/NewOrder"  //PROCESSED BY {"ars_properties.type":"factory"}
}